import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#F2F2F2",
    height: 42,
    padding: 12,
    borderRadius: 7,
    flexDirection: "row",
    gap: 12,
    overflow: "hidden",
  },
  title: {
    fontSize: 14,
    fontWeight: "600",
  },
})
